[pytest]
testpaths = tests
# The system temp directory is not always readable on Windows hosts, which
# makes every tmp_path fixture error out before the test body runs.
addopts = --basetemp=tests/_scratch/pytest
